"use strict";

// Modules
const { google } = require('googleapis');

// Loads
const 

async function fnReqData()
{
  const service = google.youtube('v3');

  const nArrQuery = Object.values(fnGetQueries());
}