"use strict";

// Modules
const { google } = require('googleapis');

// Loads

// Jobs
const serviceReq 

function fnRunService()
{
  const service = google.youtube('v3');

  const nArrQueries = Object.values(require('./query')());
}

fnRunService();